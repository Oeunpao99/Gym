# Secrets - never commit actual env files, only the .example templates
.env
.env.dev
.env.prod
.env.local

# Python
backend/.venv/
backend/__pycache__/
**/__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/

# Legacy Node app (untouched, but no need to track its local artifacts)
node_modules/
gym.db

# Runtime / editor logs
.codex-server.log
.codex-server.err.log
